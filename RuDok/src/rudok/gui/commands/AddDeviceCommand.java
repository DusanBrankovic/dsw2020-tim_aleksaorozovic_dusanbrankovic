package rudok.gui.commands;

import rudok.gui.view.painters.CirclePainter;
import rudok.repository.Page;
import rudok.repository.slot.Slot;

import java.awt.geom.Point2D;

//public class AddDeviceCommand extends AbstractCommand{

/*    Page page;
    Point2D lastPosition;
    Slot slot = null;
    //DiagramSelectionModel selectionModel;
    SlotType slotType;

    public AddDeviceCommand(Page page, Point2D lastPosition,SlotType slotType) {
        this.page = page;
        this.lastPosition = lastPosition;
        this.slotType=slotType;


    }

    @Override
    public void doCommand() {
        if (slot ==null)
            if (slotType==SlotType.CIRCLE){
                slot=CircleElement.createDefault(lastPosition, page.getElementsCount());
            }else if (slotType==SlotType.RECTANGLE){
                slot =RectangleElement.createDefault(lastPosition, page.getElementsCount());
            }

        page.
        //selectionModel.removeAllFromSelectionList();
        page.addDiagramElement(slot);
        //selectionModel.addToSelectionList(slot);
    }

    @Override
    public void undoCommand() {

    }
}*/
